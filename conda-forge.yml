github:
  branch_name: main
  tooling_branch_name: main
# v1 recipe (recipe/recipe.yaml) built with rattler-build, which performs
# overlinking checks by default.
conda_build_tool: rattler-build
conda_forge_output_validation: true
